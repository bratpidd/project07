<h2>Viewing a post. <a href="{{ path('edit_post', {post_id: post.id}) }}" target="_blank">Edit</a></h2>
<h3>Post Message:</h3>
{{ post.message }}
<h3>Tags:</h3>
{{ post.tags|join(', ') }}
<h3>Comments:</h3>
{% for comment in comments %}
    <p>{{ comment.text }}</p>
{% endfor %}
<h3>Add Comment:</h3>
{{ form_start(form) }}
    {{ form_row(form.text) }}
{{ form_end(form) }}
